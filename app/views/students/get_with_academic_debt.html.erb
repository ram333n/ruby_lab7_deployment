<h1><%=t("queries.students_with_academic_debt") %></h1>

<ul>
  <%@students.each do |student| %>
    <li>
      <%= link_to student.surname, student %>
    </li>
  <% end %>
</ul>

<%= link_to t('helpers.links.back'),
            students_path,
            :class => 'btn btn-primary' %>